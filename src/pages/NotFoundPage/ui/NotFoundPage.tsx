import * as cls from './NotFoundPage.module.scss';

export const NotFoundPage = () => {
    return (
        <div className={cls.NotFoundPage}>
            Страница не найдена
        </div>
    );
};
